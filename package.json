{
  "name": "drizzle-checkout",
  "private": "true",
  "version": "0.0.0",
  "description": "a simple test project to checkout drizzle features",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "db:generate": "npx drizzle-kit generate",
    "db:migrate": "npx drizzle-kit migrate"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/pg": "^8.11.11",
    "drizzle-kit": "^0.30.4",
    "tsx": "^4.19.2",
    "typescript": "^5"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "drizzle-orm": "^0.39.3",
    "pg": "^8.13.1"
  }
}
